import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { List } from 'docs/Components';
import Input from './Input';

<Meta title="components/Input" component={Input} />

# Input

Los Inputs o [Text Fields](https://material.io/design/components/text-fields.html#usage) permiten a los usuarios ingresar o editar texto.

<br />

<Preview>
	<Story name="Input">
		<List minWidth={170} gap={40}>
			<Input label="With floating label" />
			<Input placeholder="Placeholder" />
			<Input label="Uncontrolled" defaultValue="valor inicial" />
			<Input label="Controlled" value="value" />
			<Input disabled defaultValue="valor inicial" label="Disabled" />
			<Input label="Password" type="password" />
			<Input label="Icon" icon="location_marker" />
			<Input error errorMessage="error message" icon="box"  />
		</List>
	</Story>
</Preview>

## Props

<Props of={Input} />

## Consideraciones

El componente está preparado para ser [controlled](https://reactjs.org/docs/forms.html#controlled-components) o [uncontrolled](https://reactjs.org/docs/uncontrolled-components.html).
