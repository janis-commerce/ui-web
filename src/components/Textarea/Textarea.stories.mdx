import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { List } from 'docs/Components';
import Textarea from './Textarea';

<Meta title="components/Textarea" component={Textarea} />

# Textarea

Campo para ingresar bloques de texto (descripciones, comentarios).
La diferencia con el Input en cuanto a diseño es solamente el tamaño que se ajusta al contenido. Tiene un alto máximo de 350px con scrollbars.

<Preview>
	<Story name="Textarea">
		<List>
			<Textarea hasFloatingLabel label="Escriba aquí..." />
			<Textarea
				hasFloatingLabel
				error
				errorMessage="Error: Valor incorrecto"
				label="Error"
				defaultValue="asd"
			/>
			<Textarea hasFloatingLabel disabled label="Disabled" />
		</List>
		<br />
		<p>Puede usarse también sin la Floating Label:</p>
		<br />
		<List>
			<Textarea placeholder="Escriba aquí..." />
			<Textarea error errorMessage="Error" placeholder="Error" defaultValue="asd" />
			<Textarea disabled placeholder="Disabled" defaultValue="Contenido deshabilitado" />
		</List>
	</Story>
</Preview>

## Props

<Props of={Textarea} />
